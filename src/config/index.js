import oldConfig from './config'

const newConfig = {
  currentDevice: oldConfig.deviceName,
  mainDevice: 'main',
  sateliteDevices: [
    'crea1',
    'crea2',
    'crea3'
  ]
}

export default newConfig
